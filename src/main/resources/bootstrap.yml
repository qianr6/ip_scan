server:
  port: 19999

spring:
  datasource:
    url: jdbc:mysql://192.168.31.80:3306/sys_ip_scan
    username: root
    password: nzq19950828.MySQL
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      #底层开启功能，stat（sql监控），wall（防火墙）
      filters: stat,wall
      filter:
        #开启sql监控功能
        stat:
          slow-sql-millis: 1000
          log-slow-sql: true
          enabled: true
        #防火墙
        wall:
          enabled: true
      #监控页的配置
      stat-view-servlet:
        enabled: true
        login-username: admin
        login-password: admin
        reset-enable: false
      #监控web属性
      web-stat-filter:
        enabled: true
        url-pattern: /*
        exclusions: '*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*'
      #监控SpringBean
      aop-patterns: com.zfs.boot.*

  mail:
    #smtp服务主机  qq邮箱则为smtp.qq.com
    host: smtp.qq.com
    #服务协议
    protocol: smtp
    # 编码集
    default-encoding: UTF-8
    #发送邮件的账户
    username: 742632713@qq.com
    #授权码
    password: eghmxnnzkplsbcba
    test-connection: true
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

ali-config:
  accessId: LTAI5tB8HaxGzGw7hLrCHXLq
  accessSecret: wUqUxVGS1rA6nE8VahFpR9huxLbqTz

mybatis:
  mapper-locations: classpath:mapper/*.xml

